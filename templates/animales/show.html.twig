{% extends 'base.html.twig' %}

{% block title %}Animales{% endblock %}

{% block body %}
    <h1>Animales</h1>

    <table class="table" id="tablaShow">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ animale.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ animale.Nombre }}</td>
            </tr>
            <tr>
                <th>Especie</th>
                <td>{{ animale.Especie }}</td>
            </tr>
            <tr>
                <th>Edad</th>
                <td>{{ animale.Edad }}</td>
            </tr>
        </tbody>
    </table>

    <button id="boton"><a href="{{ path('app_animales_index') }}">back to list</a></button>

    <a href="{{ path('app_animales_edit', {'id': animale.id}) }}">edit</a>

    {{ include('animales/_delete_form.html.twig') }}
{% endblock %}
